#include <iostream>
#include<bits/stdc++.h>

using namespace std;

int main()
{
    string s;cin >>s;
    int counter = 0; vector<int>index;bool right = false,left=false;
    for(int i = 0 ; i < n ; i++)
    {
        if(s[i] == '(' && s[i+1]!= ')' )
        {
            right=true;
        }
    }
    return 0;
}
